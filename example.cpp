#include<stdio.h>

int main() {
    long long a, b;
    scanf("%lld %lld", &a, &b);
    // for(int i = 0; i < a + b; ++i) {
    //     // do nothing
    // }
    // int arr[a], arr2[b];
    printf("%lld\n", a + b);
    return 0;
}